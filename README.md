This is the required sections for each document

## Summary

This repository contains lessons learned from cloud-native experiments, Kubernetes deployments, and other hands-on projects. Each document is structured to make knowledge transfer easier.

## What I Learned

- A section on what I learned.

## Commands

**Example**

- `k3d cluster create ...` — create local K3s cluster
- `kubectl get nodes` — check node status
- `helm install ...` — install Helm charts
- `docker ps` — list running containers
- `git add . && git commit -m "..."` — commit changes
